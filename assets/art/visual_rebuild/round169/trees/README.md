# Round169 Tree Candidate Assets

Status: `normalized_candidate`

These files are candidate visual-rebuild tree assets produced during Round169. They are stored under the asset tree so future visual-layout previews can reference stable paths, but they are not wired into runtime.

## Files

- `tree_round_objects_256x320_sheet_v002.png`
  - Fixed-cell RGBA atlas.
  - Grid: `3x2`.
  - Cell size: `256x320`.
  - Pivot per cell: `(128, 300)`.
  - Godot import sidecar: `tree_round_objects_256x320_sheet_v002.png.import`.
- `tree_round_objects_256x320_pivot_proof_v002.png`
  - Proof-only image showing cell grid, pivot baseline, and bottom-center pivot.
  - Do not use as runtime art.
  - Godot import sidecar: `tree_round_objects_256x320_pivot_proof_v002.png.import`.
- `tree_round_objects_256x320_manifest_v002.json`
  - Normalization manifest and gate result.
- `normalized_256x320/tree_round_01_256x320_v002.png` through `tree_round_06_256x320_v002.png`
  - Individual fixed-cell sprite PNGs copied into the asset tree for downstream proof tooling.

## Gate Result

- All six sprites passed fixed-cell normalization.
- Edge alpha touch: `0` on all sides.
- Connected components: `1` per sprite.
- Category: `round_broadleaf_tree`.
- Recommended initial display scale for visual-layout preview: `0.60`.

## Godot Import Check

Generated by:

```bash
godot --headless --editor --path . --quit
```

Relevant import settings:

- `compress/mode=0`
- `mipmaps/generate=false`
- `process/fix_alpha_border=true`
- `process/premult_alpha=false`

The manifest paths point to the stable asset-tree atlas, pivot proof, and individual normalized PNGs. Raw source paths remain under `docs/collaboration/...` as provenance only.

## Runtime Boundary

Do not map these files into `AssetResolver`, `ThemeProfile`, or live runtime until a visual-layout preview confirms scale, depth, occlusion, and composition behavior.

These assets are not `art_target_locked`, not `runtime_visual_match`, and not final approved.

## Source Record

Full prompt, generation notes, failure lesson from v001, normalization contract, and proof records live at:

`docs/collaboration/round169_visual_rebuild_same_type_img2img_batches/tree_pipeline_test_v001/Round169_tree_prompt_to_landing_report.md`

Headless Godot preview proof:

`docs/collaboration/round169_visual_rebuild_same_type_img2img_batches/tree_pipeline_test_v001/round169_tree_godot_preview_1280x720_v002.png`
