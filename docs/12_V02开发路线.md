# V02 开发路线

> 最后更新：2026-06-05
> 当前事实来源：`todo.md`。
> 本路线从旧 0-8 阶段计划更新为当前项目事实：工程可玩闭环、可信首屏基线、5 分钟每日生活纵切、7 天回访节奏、V02.10 P1 居民回访扩展、V02.11 天气与小镇轻事件纵切、V02.12 学校-家庭中心世界地图与 A-Z 锚点规划、V02.13 全量课本内容世界化主线策划、V02.14 Home / School P0 可玩纵切、V02.15 Home / School 日常回访与学校生活轻循环、V02.16 可制作内容与体验抛光 / Playable RC Gate、V02.17 世界地图运行时落地与 26 Anchor 可视布局和 V02.18 世界地图视觉可读性与探索体验抛光已完成；下一步由 PM 建立后续阶段路线与 Ready 队列。

## 当前完成事实

StudyGame V02 已完成生活 RPG / cozy town 的工程可玩基础：

- 可探索小镇：玩家移动、触屏控制、Home / Town Plaza / Shop 等小镇区域、NPC、资源、商店和家园装饰已接通。
- 每日系统：每日问候、每日资源刷新、轻委托、今日状态和本地保存已通过 smoke。
- 我的小屋：独立小屋视图、家具摆放/旋转/收起、宠物用品、Sunny 家内反馈和保存重载已通过。
- A-Z 场景化：26 个 A-Z anchor 常驻世界结构，首批 A/B/C/D/K/O/S/T/W 已强化为小镇物件、相册收藏和新词回访路径。
- 内容合同：每日委托、NPC 对话、家具商品、anchor 内容包和新词故事都有数据合同与校验。
- 真实可玩入口：相册、商店购买、小屋家具操作等核心动作已有孩子端可见入口，并通过玩家操作级 smoke。
- 美术接入链路：`ThemeProfile` / `AssetResolver` 已能按逻辑 asset ID 接入 PNG/SVG 资源；V02.7A 已用 1280x720 与 960x540 截图建立当前 production 基线，但后续仍不得把缺少新截图证据的大改动直接标成 `approved`。
- V02.8 纵切：孩子端已能通过真实可见入口完成 Mina 委托、资源采集、奖励、商店购买、小屋摆放、Sunny 反馈和 C/O/S 回访，并注册进 `headless_runner`。
- V02.9 回访节奏：`local_day_001` 至 `local_day_007` 的今日状态、商店轮换、P0 可见路径、P1 不阻断边界和 1280x720 / 960x540 代表截图已通过。
- V02.10 P1 居民回访：Bookshop / Bear Corner / Bus Stop / Taxi marker 已有真实可见入口、两条 P1 轻回访、B/T 相册记录、P1 smoke 和双视口截图证据。
- V02.11 天气轻事件：晴天、微风、小雨、雨后已进入今日状态、NPC 问候、资源 / 商店轻变化和 S/K/B/U 天气相册线索。
- V02.12 世界地图地基：Home / School 已定为世界地图双中心；`az_world_plan` 覆盖四层环线、26 anchor 分布、17 个 reserved anchor 制作规格、Home / School 0 基础挂接和后续截图验收口径。
- V02.13 课本世界化：小学三上至六下 8 册内容已补齐为可加载数据；`textbook_world_plan` 覆盖 85 个单元摘要、P0 / P1 / P2 世界映射、Home / School 第一主线段落和合同验证。
- V02.14 可玩纵切：Home 早晨、Home-School Walk、School Gate / School Yard 和 Return / Sunny story 已接成孩子端真实可见 P0 路线，并通过 focused / headless / MCP 截图抽查。
- V02.15 日常回访：7 天 Home / School 学校生活轻循环已接入真实 `看看` 路径、Sunny return 保存、A-Z card state 和双视口截图。
- V02.16 Playable RC Gate：当前 P0/P1 核心体验已完成文本审校、操作动线抛光、关键截图复核和总 smoke；5-10 分钟试玩路径可作为后续地图落地的质量基线。
- V02.17 世界地图落地：26 个 anchor 已从规划 / 局部可玩状态推进到运行时地图布局、热点、相册落账和截图验收口径；地图上已有 A-Z 徽章作为辅助识别。
- V02.18 地图视觉抛光：已在不新增课程内容或重排 A-Z 编码的前提下，验证并提升 26 anchor、道路、区域和探索反馈的可读性、遮挡安全和孩子端找路体验。

历史的 Memory Card、Letter Snake、Voice / AI / Parent stub 仍作为技术资产和可选系统保留。它们不再驱动主循环。

## 下一阶段目标

下一步应由 PM 建立 V02.19 或后续阶段路线与 Ready 队列。V02.18 已在 V02.17 运行时落地基础上，把世界地图从“功能可达”推进到“视觉可读、好找、愿意探索”：26 个 anchor 的物件轮廓、字母徽章、道路连接、区域层次、反馈短句和相册回访已有 focused / headless 验收口径，截图仍按 MCP 或非 headless 显示驱动取证。

| 版本 | 主题 | 目标 | 完成标志 |
|---|---|---|---|
| V02.5 | 美术素材生产线 | 建立角色、宠物、场景、A-Z 物件、家具、UI、图标和音效的生产目录与首批清单 | 所有未来素材能以逻辑 asset ID 进入 `ThemeProfile` / `AssetResolver` |
| V02.6 | 策划内容生产线 | 建立 NPC、每日委托、商店轮换、节日天气和 A-Z 场景故事的生产规范 | 新内容能进入数据合同验证，继续服务生活 RPG 和记忆宫殿 |
| V02.7 | 发布前体验与质量门槛 | 固定退出设置、截图验收、移动端布局、玩家路径和正式素材替换门槛 | 每次 UI/美术/内容大改都有玩家路径与截图验收 |
| V02.7A | 美术基线重建 | 承认现有资产仍偏占位，重建 Town Plaza 首屏、基础角色、Home/Shop 和关键小物件视觉基线 | 1280x720 与 960x540 首屏像温暖小镇，不像占位原型或调试网格 |
| V02.8 | 每日小镇生活纵切 | 将 NPC、轻委托、地图行动、奖励反馈、商店/小屋使用和 A-Z 地点回访串成 5 分钟日常 | 孩子端可从真实入口完成一段“小镇生活”，并通过 smoke 与截图验收 |
| V02.9 | 一周回访节奏 | 将每日状态、居民轻委托、商店轮换、小屋反馈和 A-Z 回访组织成 7 天低压力节奏 | 连续 7 个本地 day_key 均有可玩内容、无惩罚文案、无主流程断点，并通过合同 / 操作级 smoke |
| V02.10 | P1 居民回访扩展 | 将故事熊 / Bookshop、巴士哥哥 / Bus Stop 从预收入口推进为可见支线回访 | Bookshop / Bus Stop 有真实可见入口、P1 轻回访、相册 / A-Z 记录、双视口截图和 smoke；仍不阻断 P0 主路径 |
| V02.11 | 天气与小镇轻事件纵切 | 将晴天、微风、雨后、小雨等 P0 氛围事件数据化并接入今日状态、问候、资源、商店和 A-Z 线索 | 多个本地 day_key 能显示不同天气 / 轻事件，P0 主路径不阻断，无倒计时 / 错过 / 打卡压力，并通过 focused/headless 与代表截图 |
| V02.12 | 学校-家庭中心世界地图与 A-Z 锚点规划 | 将 Home / School 定为世界地图中心，并把 26 个 A-Z anchor 分布到中心、第一圈、第二圈和远郊预留 | `az_world_plan` 数据合同覆盖 26 个 anchor、Home/School 中心路线、远郊边界和 0 基础 / 课本挂接位，并通过 focused/headless 验证 |
| V02.13 | 全量课本内容世界化主线策划 | 将小学三上至六下课本内容补齐为世界化数据源，按 Home / School 第一主线和 P0/P1/P2 分层映射到 Sunshine Town | `textbook_world_plan` 数据合同覆盖 8 册来源、85 个单元摘要、世界映射、P0 近场边界和禁用压力文案，并通过 focused/headless 验证 |
| V02.14 | Home / School P0 可玩纵切 | 将 Home morning、Home-School Walk、School Gate / School Yard 和回家 / Shop bridge 串成第一条孩子端真实可见主线 | 玩家可从 Home 早晨出发，经可见 `看看` 入口完成家庭、上学路、校门 / 操场和回家反馈；不出现课程页、测试、背诵、远郊硬依赖，并通过操作级 smoke 与双视口截图 |
| V02.15 | Home / School 日常回访与学校生活轻循环 | 把 V02.14 的单次 P0 路线扩展为 7 天低压力回访节奏，覆盖 School Gate 问候、School Yard 小发现、Home-School Walk 差异和回家 Sunny 反馈 | 7 个本地 day_key 至少有 3 类学校生活差异；孩子端仍通过真实 `看看` / 移动 / 相册路径触发；A-Z School 线自然复现；不出现课堂页、作业、测试、打卡、迟到或分数 |
| V02.16 | 可制作内容与体验抛光 / Playable RC Gate | 将 V02.8-V02.15 的每日小镇、Home / School、天气、P1 轻回访、商店、小屋、Sunny 和相册整理为 5-10 分钟可试玩质量门槛 | 文案统一温暖短句；核心入口顺手可关闭；Town Plaza、Home、Shop、School Gate、School Yard、Album、Settings 双视口截图复核；总 smoke 覆盖启动到设置，不出现工程文案、课程压力或隐藏入口验收 |
| V02.17 | 世界地图运行时落地与 26 Anchor 可视布局 | 将 `az_world_plan` 的 26 个 anchor、四层环线和安全边界转为运行时地图布局、热点、相册落账和截图验收 | 26 个 anchor 都有地图坐标 / 可见物件 / 入口状态；P0 中心路线优先可玩；first / second ring 分层可达或预览；X/Z 远郊只作边界预览；focused/headless 与双视口截图通过 |
| V02.18 | 世界地图视觉可读性与探索体验抛光 | 将 V02.17 的运行时地图做成孩子能看懂、找得到、可回访的探索界面 | 26 anchor 的物件轮廓和字母徽章在 1280x720 / 960x540 下可读；Home / School / Town / Far Edge 区域层次清楚；热点不互相吞掉；反馈和相册不课程化；双视口截图和操作级 smoke 通过 |

## 四条并行路线

### 工程路线

- 维护现有可玩闭环，不把已完成系统改回调试面板或隐藏 contract 按钮。
- 所有“可玩功能”必须有孩子端可见入口，且至少一个测试通过可见按钮或面板按钮触发。
- 玩法脚本不得硬编码具体素材路径，只能通过逻辑 asset ID 和 `AssetResolver` 请求资源。
- 新增数据内容优先改 JSON / 数据合同 / focused test，避免把内容规则重新塞回 `scripts/main.gd`。
- V02.12 的世界规划先进入独立 `az_world_plan` 数据合同；未通过规划合同前，不把 26 个 anchor 一口气写成运行时主线任务。

### 策划路线

- NPC 问候、关系阶段、轻委托、商店轮换、节日天气和 A-Z 场景故事要形成可复用生产规范。
- 每条每日委托必须有 NPC、目标、奖励、关系反馈、重复规则、儿童安全文案和可见操作入口。
- 经济设计保持非惩罚：买不起只给温和反馈，不制造打卡压力、损失惩罚或强消耗循环。
- 英语继续作为环境层、收藏层、NPC 短句和记忆宫殿故事层，不作为考试、课程表或主线门槛。
- 后续课本主线从 Home / School 的 0 基础桥接开始，知识点以场景物件、NPC 短句、相册故事和轻委托挂接到 anchor，而不是以课程地图呈现。
- V02.14 P0 只选 Home、Home-School Walk、School Gate、School Yard、Shop 和 Sunny 角落；Bookshop、Bus Stop、Park、Theatre、Music Corner、Far Edge 继续作为 P1/P2，不阻断第一条可玩主线。
- V02.15 继续只强化 Home / Walk / School Gate / School Yard / Home Return，不新增课程 UI；School 是生活地点、问候地点和操场回访地点，不是课堂或考试空间。
- V02.16 只整理现有 P0/P1 体验，不新增课程 UI、不扩 Bookshop / Bus Stop 为 P0、不改变 A-Z anchor 结构；所有文本审校以温暖、生活化、短句为准。
- V02.17 只把既有 `az_world_plan` 运行时落地，不重新命名字母、不改变 `anchor_id` / `core_word` / `route_order`，也不要求孩子按 A-Z 顺序走完整路线。
- V02.18 只做地图视觉可读性、导航和探索反馈抛光，不新增课本主线、不扩远郊 P0、不把字母徽章做成主任务或完成率。

### 美术路线

- 初期只采用单一 storybook / cozy town 风格，不并行扩展多套主题。
- 所有素材需要用途、逻辑 asset ID、尺寸/比例、状态、替换优先级和验收截图。
- 首批素材范围覆盖角色/NPC、Sunny/宠物、小镇场景、A-Z 锚点物件、家具与家园、UI 与图标、音效与环境氛围。
- UI 和 HUD 逐步从纯文字表达转向图标+短文本表达，但必须保证移动端可读和可触控。
- `production` 只表示资源可进入游戏集成，不等同于美术最终质量；`approved` 必须有 PM / Art Direction 确认和 1280x720、960x540 截图证据。
- V02.7A 前不得继续把缺少截图证据的生成图或程序占位图当作产品视觉完成。
- V02.16 截图复核必须区分 `production` 和 `approved`：可集成不等于可批准，缺少 1280x720 与 960x540 证据的视图不得标为 `approved`。
- V02.17 地图资产必须按 logical asset ID 接入；每个 anchor 在 960x540 下至少能读出主轮廓，不能依赖小字或裸字母牌证明存在。
- V02.18 的地图抛光必须继续以生活物件为主、字母徽章为辅助；任何新视觉层都要在双视口截图中证明无遮挡、可读且不压住 HUD / 交互按钮。

### QA 路线

- 数据合同继续拦截重复 ID、非法价格、儿童安全文本、缺失记忆宫殿字段和覆盖核心 A-Z anchor。
- 玩家路径 smoke 继续覆盖小镇、背包、商店、小屋、相册、anchor、NPC、资源和家具操作。
- 截图验收覆盖 1280x720 和较小横屏视口，检查遮挡、文字溢出、安全区、按钮尺寸和关闭路径。
- 发布前质量门槛必须包含退出/设置入口、移动端触控复核和首批正式或半正式素材替换。
- V02.8 纵切验收必须覆盖从启动到完成一段日常生活的玩家路径，不能只验证服务或隐藏 contract 方法。
- V02.9 已完成 7 天回访节奏；后续改动必须继续保持无连续登录奖励、错过惩罚或限时压力。
- V02.10 只允许把 Bookshop / Bus Stop 做成 P1 支线回访；未完成真实入口、截图和儿童安全验收前，不得把它们写入 P0 主流程硬依赖。
- V02.11 只做天气 / 轻事件的生活化纵切，不做真实天气联网、运营限时活动、连续登录、补签、排行榜或节日奖励墙。
- V02.12 必须验证 Home / School 为中心区域，A/C/D/W 覆盖 Home 线，E/G/K/N/R/Y 覆盖 School 线，X/Z 等远郊锚点不成为 P0 主流程硬依赖。
- V02.13 必须验证三上至六下 8 册内容源齐备，课本内容只进入内部数据和世界映射，不接孩子端课程页、单元页、测试页或主流程门槛。
- V02.14 必须验证 Home / School 主线每一步都有孩子端真实可见入口，P0 不引用远郊、课程页、测试页、背诵页、词表墙、倒计时或必须完成文案；截图验收继续覆盖 1280x720 和 960x540。
- V02.15 必须验证 7 天 Home / School 回访不会破坏 V02.8-V02.14 旧闭环；学校生活差异只能通过环境、NPC 短句、相册线索和回家故事表达，不得出现老师训导、作业检查、课程页、测验、打卡、迟到、倒计时、分数或家长报告。
- V02.16 必须验证试玩版总路径：启动 -> Mina / 资源 -> Shop -> Home -> Sunny -> Home-School Walk -> School Gate -> School Yard -> Return Home -> Album -> Settings；所有步骤必须来自孩子端真实可见入口，并通过 headless smoke 与双视口截图验收。
- V02.17 必须验证 26 anchor 运行时覆盖：P0 Home / School 中心可走可看，first / second ring 不阻断 P0，far edge X/Z 不成为主流程依赖；所有可玩项必须从孩子端真实可见入口触发，相册落账不能只靠服务直调。
- V02.18 必须验证 26 anchor 可读性和探索体验：截图覆盖 Home anchors、School line、first ring、second ring、far edge、相册；测试覆盖字母徽章存在、热点优先级、旧 P0/P1 路径不阻断和儿童安全文案。

## 持续门槛

- 每个可玩功能必须有玩家可见入口，隐藏 contract 按钮不能作为完成依据。
- 每个素材必须有稳定逻辑 asset ID，不能由玩法脚本硬编码具体路径。
- 每个新词必须绑定 `letter`、`core_anchor_id`、`world_place_id`、`story_memory`、`visual_hook` 和 `review_path`。
- 每次 UI 或美术大改必须补截图验收，不能只跑 headless。
- 儿童端不得出现失败惩罚、课程压力、家长报告口吻、开放陌生人社交或工程调试文案。
