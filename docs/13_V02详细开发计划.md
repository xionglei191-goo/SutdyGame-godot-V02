# V02 详细开发计划

> 状态：旧学习闭环计划归档。
> 2026-06-07 二次视觉复核更新：V02.38 Round155-Round159 已完成 no-full-map modular runtime 工程纵切，但对照 `docs/collaboration/artpass003_visual_direction/` 后仍显著偏离目标。V02.38 项目级状态重解释为 `visual_scaffold`，未授予 `final_approved`，也不足以解锁 StoryBatch；当前 Ready 改为 `V02-VISUALREBUILD-002 1280 目标画面与 Godot 可执行视觉合同`。
> 当前新事实来源：`todo.md`、`docs/12_V02开发路线.md`、`docs/10_美术风格与换肤预留.md`、`docs/14_内容基线整理与首批内容规划.md`。
> 本文件中关于 Home/Pet/Shop/Coins/Memory Card/Letter Snake 作为第一阶段主循环的内容，只作为已实现技术资产和历史参考，不再作为后续任务优先级。
> V02.20-V02.23 的历史 proof 与 Round119 逐项判定仅作为旧阶段可玩与可读证据；自 V02.38 起统一解释为 `functional_pass`，不代表 `final_approved`。Round159 的 `visual_candidate` 也在二次复核后降格为 `visual_scaffold`。`final_approved` 必须等 V02.39 target frame、runtime visual match、真实入口和 PM / Art Direction 复核通过后重新授予。

## 当前执行入口

- `todo.md`：唯一任务状态事实来源；Round153 已完成第二批 production 资产接入，Round155-Round159 已完成 V02.38 modular visual scaffold；当前 Ready 为 `V02-VISUALREBUILD-002 1280 目标画面与 Godot 可执行视觉合同`，`V02-STORYBATCH-004/005` 继续阻塞。
- `docs/12_V02开发路线.md`：当前事实路线，覆盖已完成可玩闭环、V02.5-V02.22、V02.23 体验批准线和 V02.24-V02.27 收口。
- `docs/10_美术风格与换肤预留.md`：美术生产基线，定义素材分类、命名、逻辑 asset ID 和验收截图要求。
- `docs/14_内容基线整理与首批内容规划.md`：策划内容生产基线，定义 NPC、每日委托、商店轮换、节日天气和 A-Z 场景故事生产规则。

## V02.39 当前执行计划摘要

V02.39 是 Godot 视觉生产体系重建，不是 V02.38 的截图补丁。二次视觉复核确认，当前核心问题是生产模型仍然由 `world_map` 数据、cell、place、anchor、NPC 和功能入口驱动画面，再叠加美术资产；这会持续产出“系统索引式地图”，而不是 `artpass003_visual_direction` 要求的 home-centered cozy town target frame。

本阶段的目标生产顺序固定为：`target 1280 frame -> visual layout / layer / perspective / density contract -> unified asset kit -> Godot runtime static-frame match -> gameplay binding -> art-direction gate`。`V02-STORYBATCH-004/005` 在 `runtime_visual_match` 前不得恢复 Ready。

| 阶段 | 目标 | 完成门槛 |
|---|---|---|
| V02-VISUALREBUILD-001 Godot 视觉生产体系重建路线与任务包 | 将二次视觉复核结论写入 docs 12-15、todo、lessons 和 Round160 任务包，切换唯一 Ready | V02.38 被重解释为 `visual_scaffold`；StoryBatch 阻塞；V02.39 任务队列、状态语义和验收门槛同步 |
| V02-VISUALREBUILD-002 1280 目标画面与 Godot 可执行视觉合同 | 输出首屏 target gameplay frame / paintover 或同等目标图，并拆成 Godot 可执行合同 | 明确 home-centered 构图、camera、depth bands、layer order、asset scale、negative space、UI safe area、A-Z 可见优先级、禁止残留和截图比对规则；达到 `art_target_locked` |
| V02-VISUALREBUILD-003 Logic Map / Visual Layout 分层方案 | 把 `world_map` 逻辑事实与首屏视觉布局分离 | `world_map` 继续管 cell / collision / interaction / A-Z ID / save；Visual Layout 管 visual anchors / depth / proxy / occlusion / focus state；不破坏旧真实入口 |
| V02-VISUALREBUILD-004 首屏统一环境资产包与 composition kit | 基于 target frame 生产或整理统一环境资产包 | 资产风格、比例、光源、透视、边缘、阴影、透明背景和 logical asset ID 一致；A-Z 只以生活 prop / 环境线索进入首屏 |
| V02-VISUALREBUILD-005 TownStage runtime visual match 纵切 | 先匹配一张静态 gameplay frame，再恢复必要交互代理 | 1280 runtime screenshot 与 target frame 并排复核通过；旧 scene/context 残留清零；HUD / actor / anchor / label 噪声受控；达到 `runtime_visual_match` |
| V02-VISUALREBUILD-006 Art-direction gate 与 StoryBatch 解锁判定 | 用同轮 proof、真实入口 smoke 和 PM / Art Direction 判断是否恢复内容扩张 | 只有 `runtime_visual_match` + 真实入口不退化 + A-Z 稳定 + 文本安全通过后，才允许 `V02-STORYBATCH-004` 从阻塞转 Ready |

## V02.38 收口事实与二次复核结论

V02.38 采用强纠偏：Round154 先暂停 V02.37 后续 runtime / approved 推进，完整重写当前路线、计划、内容基线、接管计划和台账，把 artpass003_visual_direction 升为硬门槛。Round159 已完成 no-full-map modular runtime gate，证明 `world_map_base_1280.png` 可以退出 final runtime 主画面；但二次视觉复核确认这只是工程脚手架，不足以恢复 `V02-STORYBATCH-004` Ready。后续需要 V02.39 先建立 target frame 和 visual layout contract。

Round155-Round159 已完成本表 `002..006`。Round159 文档内 lane 结果曾写为 `visual_candidate`，但用户二次视觉复核确认当前结果仍远离目标方向，因此项目级重解释为 `visual_scaffold`：工程 modularization 有价值，但不能恢复第二批 runtime smoke，也不能把任何视图标为 `final_approved`。

| 阶段 | 目标 | 完成门槛 |
|---|---|---|
| V02-VISUALRECOVERY-001 全面纠偏与文档重写 | 重写 docs 12-15、todo、lessons 和 Round154 任务包，切换唯一 Ready | Round154 暂停 story batch、approved 语义、artpass003 硬门槛、full-map background 禁用目标全部同步 |
| V02-VISUALRECOVERY-002 Modular Visual System 规格 | 固定 tile + region chunk + building prefab + prop/anchor + actor + glass UI 的生产规格 | `world_map_base_1280.png` 只作 reference；第一屏最小资产包和 runtime layer 目标明确 |
| V02-VISUALRECOVERY-003 首屏统一资产包生产 | 生产 Town Plaza / Home / Shop / School line 的首批统一资产包 | 每个 asset 有 logical asset ID、尺寸、透明背景、统一相机/光源/比例和 provenance |
| V02-VISUALRECOVERY-004 TownStage 无整图底图渲染纵切 | 用 modular layers 拼出首屏，不再依赖 full-map background 主渲染 | 旧移动、`看看`、NPC、资源、Shop、Home、A-Z 路径不退化；runtime proof 证明无 full-map background 主画面 |
| V02-VISUALRECOVERY-005 HUD / Anchor / Actor 视觉降噪 | 统一 HUD、底栏、prompt、A-Z badge、玩家/Sunny/NPC 比例 | A-Z 默认 prop-first，字母 badge 只作辅助；UI 不再用大白条压画面 |
| V02-VISUALRECOVERY-006 1280 Final Visual Gate | 对照 artpass003 判定 Town / Home / Shop / School / UI | Round159 只证明 no-full-map modular scaffold 与真实入口未退化；二次复核后不授予 `visual_candidate` / `final_approved`，转入 V02.39 |

## 当前 PM 执行计划摘要

> 历史计划以下保留用于追溯；凡与 V02.39 当前执行计划冲突时，以本文件顶部和 `todo.md` 的 V02.39 状态为准。

V02.21 已把 V02.20 的部分 playgate 缺口落为运行时返修：空间分层、热点优先级、Home 可见家具操作、Shop / School line 到达反馈、3-5 分钟自由生活 smoke 和同轮 1280 proof 已完成。Round106 二次判定中 Album 可标 V02.21 playgate approved，但 Town / Home / Shop / School Gate / School Yard / Settings 仍保留 needs_fix；这说明下一阶段不应简单继续堆美术或内容，而应把底层形态推进为更接近 Animal Crossing-like 的隐藏网格生活小镇，并同步拆分 scenes，让可居住、可装饰、可回访的小镇能长期扩展。自 2026-06-06 起，后续所有未完成任务的阻塞性视觉验收只要求 1280x720；960x540 暂缓到全部开发完成后的版本适配专项，不作为中间阶段完成门槛。

| 阶段 | 目标 | 完成门槛 |
|---|---|---|
| V02-PLAYGATE-001 V02.20 可居住小镇重建路线、任务队列和 Ready 规划 | 固定 V02.19 1280 art baseline 收口口径、V02.20 队列、不可改范围和 Ready | `docs/12`、`docs/13`、`docs/15`、`todo.md` 和 Round100 任务包同步；V02.19 不标 product complete |
| V02-PLAYGATE-002 首屏可居住小镇 QA 审计与缺口清单 | 审计 Town Plaza、Home、Shop、School Gate、School Yard、Album、Settings 的居住感、动线、遮挡和儿童安全视觉 | 只审计现有 proof / 文档 / 台账，不改 runtime、tests、assets 或 data；缺口分为阻塞 playgate、可后续优化、版本适配专项 |
| V02-PLAYGATE-003 启动到 5 分钟自由生活 playgate smoke 规格 | 固定启动、移动、看看、NPC、资源、商店、小屋、Sunny、相册、设置的真实入口 smoke 规格 | 每一步来自孩子端真实可见入口，不用隐藏 contract 按钮 |
| V02-PLAYGATE-004 居民、资源、家具和 anchor 空间分层返修方案 | 固定空间层级、热点优先级、生活用途可读性的返修建议 | 不改 A-Z ID / route_order，不让资源 / NPC / anchor 热点互相吞掉 |
| V02-PLAYGATE-005 HUD / 底栏 / 弹层 / 相册可读可触摸返修方案 | 固定常驻 UI 与弹层的 1280x720 可读可触问题清单和返修建议 | 无明显遮挡、文字溢出或关闭路径不清；960x540 保留到全部开发完成后专项适配 |
| V02-PLAYGATE-006 孩子端文本和生活反馈 playgate 审校 | 审校 HUD、NPC、Sunny、商店、小屋、相册、设置和反馈文本 | 温暖短句、生活化反馈，无课程、测试、评分、打卡、倒计时、错过或工程文案 |
| V02-PLAYGATE-007 V02.8-V02.19 旧玩家路径回归矩阵 | 建立旧玩家路径保留 / 退化 / 待返修矩阵 | 每日小镇、小屋、A-Z、天气、Home / School、P1、Playable RC 和 art pass 后路径均有结论 |
| V02-PLAYGATE-008 1280 release-candidate 截图包和 approved 判定 | 输出 1280 release-candidate 截图包和 `production` / `approved` 判定记录 | 只有 PM / Art Direction 截图判断通过的视图才能标 `approved` |
| V02-PLAYGATE-009 V02.20 playgate 收口与下一阶段决策 | 收口 V02.20 并决定进入内容扩展、移动适配专项或继续居住感返修 | 完成记录、Ready 状态和 lessons 复核同步 |

V02.20 已完成 PLAYGATE-001..009 文档队列并收口为 V02.21 输入；后续实现任务必须继续沿用 V02.19 的 logical asset ID、ThemeProfile / AssetResolver、A-Z anchor 稳定结构和 1280x720 判断口径。

V02.21 LIVEGATE-001..005 已完成并收口为 V02.22 输入。Round108 已完成 `V02-HIDDENGRID-002 TownStage scene 抽离`，Round109 已完成 `V02-HIDDENGRID-003 Actor / Interactable scene 抽离`，Round110 已完成 `V02-HIDDENGRID-004 UI scenes 抽离`，Round111 已完成 `V02-HIDDENGRID-005 户外装饰、资源 2.0 与 NPC routine 隐藏网格纵切`，Round112 已完成 `V02-HIDDENGRID-006 3-5 分钟回归、1280 proof 和阶段收口`，Round113 已完成 `V02-HIDDENGRID-007 Scene-native map + UI panel refactor`：地图舞台、Player / NPC / Resource / Anchor / Place hotspot、TownHUD / TownFooter、Backpack / Settings / Shop / MemoryAlbumOverlay / HomeRoom panel scenes、户外装饰、资源刷新摘要、NPC routine fallback、地图 authoring scene、runtime fixed layers、final smoke 和 1280 proof 均已接入，并用旧 free life smoke、地图可读性、26 anchor 路径、Playable UI 和新增 split / authoring / layer tests 证明不退化。V02.22 可阶段收口，但不标 product complete。

V02.23-V02.28 的执行原则是先批准、再加固、再工具化、再小批量内容、补齐场景内 Map Editor，最后用真实生产 dogfood 证明工具链能服务后续内容扩展。`V02-EXPAPPROVAL-001` 已完成 PM 任务包、文档链路、判定矩阵和台账同步；`V02-EXPAPPROVAL-002` 至 `V02-EXPAPPROVAL-005` 已完成六个待返修视图的局部 proof；`V02-EXPAPPROVAL-006` 已完成 Round119 同轮 1280 RC proof、真实入口 smoke、Album 回归和逐项判定，Album、Town Plaza / World Map、Home、Shop、School Gate、School Yard、Settings 在当时口径下曾升级为 `approved`，V02.38 起仅保留为 `functional_pass` 历史证据。`V02-HOMEPLAZA-001` 至 `V02-HOMEPLAZA-005` 已完成 Round120-Round124 Home / Town Plaza 居住感加固和阶段收口，但不标 product complete；`V02-MAPAUTH-001` 至 `V02-MAPAUTH-006` 已完成 Round125-Round130 地图 Authoring 工具实用化并阶段收口；`V02-CONTENTBATCH-001` 至 `V02-CONTENTBATCH-004` 已完成内容小批量并收口 V02.26；`V02-MAPEDITOR-001` 至 `V02-MAPEDITOR-010` 已完成图层 / Inspector / Place / cell painting / resource / NPC routine / A-Z anchor 迁移、全量回归、UI 可用性降噪、左侧控制栏视觉布局二次整理、直接拖拽编辑和字母可见性返修；`V02-MAPDOGFOOD-001` 至 `V02-MAPDOGFOOD-005` 已完成 Map Editor 小批量真实生产、孩子端复核、生产手册、focused/full 回归、Godot 启动和 1280 proof。`tests/test_v0223_ui_panel_scene_split.gd` 属于 Round113 HIDDENGRID-007 的 panel scene 回归证据，不能作为 V02.23 product approval 证据。

| 阶段 | 目标 | 完成门槛 |
|---|---|---|
| V02-EXPAPPROVAL-001 V02.23 体验批准线 PM 任务包与判定矩阵 | 固定批准事实、任务队列、截图清单、禁改范围和下一项 Ready | Round114 任务包、docs/12-15 和 `todo.md` 同步；Album-only approved 和六项 needs_fix 事实明确；不改 runtime |
| V02-EXPAPPROVAL-002 Town Plaza 首屏生活密度与 anchor 降噪返修 | 返修 Town Plaza / World Map 的居民、资源、anchor、place 层次和首屏生活密度 | 真实入口 smoke 和 1280 proof 显示噪声下降；A-Z ID / route_order 不变 |
| V02-EXPAPPROVAL-003 Home 居住密度与小屋视觉批准返修 | 返修 Home 默认生活角、Sunny 反馈、家具状态和小屋视觉密度 | Round116 已完成；Home 可见操作不退化；1280 proof 可判定；不自动 approved |
| V02-EXPAPPROVAL-004 Shop / Settings glass UI 可读可触批准返修 | 返修复杂地图背景上的 Shop / Settings translucent glass 可读性、触控和关闭路径 | Round117 已完成；购买、休息确认、回安全位置路径可见可触；1280 proof 可判定；不自动 approved |
| V02-EXPAPPROVAL-005 School Gate / School Yard 生活地点化与噪声返修 | 降低 School line anchor / HUD 噪声，强化校门和操场生活地点感 | Round118 已完成；不课程化、不测试化；School Gate / Yard 1280 proof 可判定；不自动 approved |
| V02-EXPAPPROVAL-006 1280 RC 截图包、真实入口 smoke 与逐项 approved 判定 | 导出同轮 proof 并逐项判定 Album、Town、Home、Shop、School Gate、School Yard、Settings | Round119 已完成；同轮 proof、真实入口 smoke 和 PM / Art Direction 逐项判定通过；headless 或旧 proof 不自动批准 |
| V02-HOMEPLAZA-001 V02.24 PM 路线与禁改边界 | 固定 Home / Town Plaza 居住感加固任务队列、禁改范围、验收矩阵和下一项 Ready | Round120 已完成；Round121 已完成 V02-HOMEPLAZA-002；Round122 已完成 V02-HOMEPLAZA-003；Round123 已完成 V02-HOMEPLAZA-004；Round124 已完成 V02-HOMEPLAZA-005；Round125-Round130 已完成 V02-MAPAUTH-001..006；Round131-Round134 已完成 V02-CONTENTBATCH-001..004；Round135-Round138 已完成 V02-MAPEDITOR-001..010；Round139-Round140 已完成 V02-MAPDOGFOOD-001..005；当前无 Ready |
| V02-HOMEPLAZA-002..005 V02.24 Home / Town Plaza 居住感加固 | HomeRoom、Town Plaza 停留点、NPC routine 和回归收口 | Round124 已完成；不改 HomeDecorationService 存档结构，不显示格子 / 坐标 / 占格术语 |
| V02-MAPAUTH-001..006 V02.25 地图 Authoring 工具实用化 | 错误列表、验证按钮、安全写回、marker 增删、A-Z 删除保护、移动联动和 runner 集成 | 双阶段提交；非法不写、失败不毁文件、26 A-Z 不退化、runtime 仍读 JSON |
| V02-CONTENTBATCH-001..004 V02.26 内容生产小批量 | NPC routine、资源刷新点、A-Z 回访和 Shop / School Yard 小事件 | 小批量合同、focused tests 和 full runner 通过；避免复用同一 `core_anchor_id` 触发覆盖风险 |
| V02-MAPEDITOR-001..010 V02.27 Map Editor 完整化 | 场景内工具补齐图层、Inspector 输入编辑、Place、cell painting、resource、NPC routine、A-Z anchor 迁移、UI 可用性降噪、视觉布局二次整理、直接拖拽提交和字母可见性返修 | 不进孩子端 runtime；三份 JSON 分文件安全保存；V02.27 focused tests、MAPEDITOR-010 可用性 / 直接编辑测试、full runner 和 Godot 启动通过 |
| V02-MAPDOGFOOD-001 V02.28 PM 路线、禁改边界和任务包 | 固定 Map Editor 实战生产验证队列、验收矩阵和下一项 Ready | Round139 已完成；docs 12-15、`todo.md` 和 Round139 任务包同步；本轮不改 runtime/data/tests |
| V02-MAPDOGFOOD-002 Map Editor 小批量生产实战 | 使用场景内 Map Editor 完成少量 Place / resource / NPC routine 生产修改 | Round140 已完成；新增 Story Bench / Ribbon Corner，移动 ribbon resource，微调 shopkeeper routine；修改来自候选 state、Validate 和安全写回；正式 JSON 可加载；不改 A-Z 核心字段 |
| V02-MAPDOGFOOD-003 孩子端真实入口复核 | 验证 dogfood 内容在孩子端可见、可达、可关闭并不遮挡核心体验 | Round140 已完成；真实移动 / `看看` / resource / NPC 路径通过；不出现坐标、格子、编辑器或调试文案 |
| V02-MAPDOGFOOD-004 内容生产手册与操作边界 | 输出 Map Editor 内容生产手册和 A-Z 保护规则 | Round140 已完成；`docs/collaboration/Round140_V02.28_MAPDOGFOOD-002-005实战生产验证收口.md` 写明增改、验证、保存、回滚注意和 A-Z 锁定字段 |
| V02-MAPDOGFOOD-005 回归、1280 proof 与阶段收口 | 用 focused tests、full runner、Godot 启动和 1280 proof 收口 dogfood 阶段 | Round140 已完成；focused dogfood、V02.27 editor 回归、full runner、Godot 启动和 1280 proof 通过；不标 product complete |
| V02-STORYMOTIONART-001 V02.29-V02.36 长远路线与任务包 | 将完整故事线、当前地图适配、行走动画、Animal Crossing-like 操控、美术生产规格和运行时纵切拆成多轮路线 | Round141 已完成；`docs/collaboration/Round141_V02.29-V02.36_STORYMOTIONART长远路线与任务包.md`、docs 12-15 和 `todo.md` 同步；下一轮 Ready 为 `V02-STORYLINE-002` |
| V02-STORYLINE-002 全量故事线总纲与章节骨架 | 输出 P0/P1/P2 全量故事总纲、A-Z anchor 故事索引和首批资产需求 | Round142 已完成；`docs/collaboration/Round142_V02.29_STORYLINE-002全量故事线总纲与章节骨架.md` 覆盖 26 anchors、章节骨架、story memory、visual hook、review path 和首批资产需求 |
| V02-STORYLINE-003 当前地图适配与内容生产蓝图 | 将故事总纲落到当前 world_map、Place、interaction、resource、NPC routine 和 A-Z anchor | Round143 已完成；`docs/collaboration/Round143_V02.30_STORYLINE-003当前地图适配与内容生产蓝图.md` 列出 direct_runtime、asset_only、Map Editor 候选和后续 Motion / Controls / Artpipe 输入 |
| V02-MOTION-001 行走动画与角色动作规格 | 输出 player / NPC / Sunny 的 4 向基础动作、8 向预留、sprite sheet 和状态机规格 | Round144 已完成；帧数、pivot、尺寸、命名、logical animation ID 和 proof 口径清楚 |
| V02-CONTROLS-001 Animal Crossing-like 操控规格 | 输出连续移动、镜头跟随、隐藏网格、靠近 prompt、虚拟摇杆 / tap move / 键鼠手柄策略 | Round145 已完成；可直接拆给 Godot 实现 |
| V02-ARTPIPE-001 Tile map / 场景 / 故事资产生产规格 | 输出 tile、edge、prop、story object、character motion、UI feedback 的资产 backlog | Round146 已完成；每项有 logical asset ID 和验收截图点 |
| V02-ARTPIPE-002 Map Editor / AssetResolver 接入规范 | 输出 tile / prop / sprite sheet 的导入、命名、ThemeProfile、editor marker 和 runtime proof 规则 | Round147 已完成；production 不自动等同 approved |
| V02-ARTPROD-001 首批美术资产生产与接入 | 生产并接入首批 story prop / character motion / pet motion / UI feedback / tile edge production 资产 | Round148 已完成；ThemeProfile / AssetResolver / metadata / story prop marker validator 已接入；approved 仍待 1280 proof |
| V02-RUNTIME-ANIM-001 动作操控运行时纵切 | 接入连续移动、4 向动画、镜头跟随、prompt feedback 和真实入口 smoke | Round149 已完成；player motion sheet、keyboard hold、camera smoothing、prompt glow / tap ripple、focused smoke、full runner 和 1280 viewport proof 通过 |
| V02-STORYSLICE-001 故事线 + 美术 + 动作整合纵切 | 第一批 P0 故事在新操控和新资产下可玩 | Round150 已完成；四个 story prop runtime 可见可交互，A/B/H/N/R/Y 六个 anchors 落账，Story Bear / branch 轻联动、focused/full headless 和 1280 proof 通过 |
| V02-STORYBATCH-001 V02.37+ 内容批量生产与批准线规划 | 固定第二批 story prop / A-Z 回访、production 资产接入、runtime smoke、1280 proof 和 approved 判定队列 | Round151 已完成；`docs/collaboration/Round151_V02.37_STORYBATCH-001内容批量生产与批准线规划.md`、docs 12-15 和 `todo.md` 同步 |
| V02-STORYBATCH-002 第二批 story prop / A-Z 回访内容包 | 输出 C/D/G/K/O/S/W 内容候选、story memory、visual hook、review path、asset needs 和儿童安全边界 | Round152 已完成；下一轮输入已进入 production 资产接入 |
| V02-STORYBATCH-003 第二批 production 资产与接入 | 生产并接入第二批 story prop production 资产 | Round153 已完成 7 张 PNG、logical asset ID、ThemeProfile / AssetResolver、story prop JSON、Map Editor marker 和 focused/full 验证；二次视觉复核后 `V02-STORYBATCH-004/005` 继续阻塞，等待 V02.39 runtime visual match |

| 阶段 | 目标 | 完成门槛 |
|---|---|---|
| V02-LIVEGATE-001 空间分层与热点优先级返修 | 按 PLAYGATE-004 规则修复居民、资源、anchor、place 空间层级与 `看看` prompt 稳定性 | 不改 A-Z ID / route_order；Town / Home / Shop / School line 不互吞；focused/headless 验证通过 |
| V02-LIVEGATE-002 Home 居住感和直接家具操作返修 | 把小屋从功能网格推进到可居住房间与可见家具操作 | 家具选择 / 摆放 / 旋转 / 挪动 / 收起和 Sunny 反馈来自可见操作；保存重载通过 |
| V02-LIVEGATE-003 Shop / School line 到达感与局部 proof | 补 Shop、School Gate、School Yard 的地点到达感和同轮 1280 RC proof | 三处地点生活用途清楚，无课程化；截图和旧路径回归通过 |
| V02-LIVEGATE-004 3-5 分钟自由生活 smoke 实现 | 将 PLAYGATE-003 规格落为 focused test 和 runner 集成 | 启动、移动、看看、NPC、资源、商店、小屋、Sunny、相册、设置全部来自真实入口 |
| V02-LIVEGATE-005 1280 RC 截图包与二次 approved 判定 | 重新导出 Town、Home、Shop、School Gate、School Yard、Album、Settings 同轮 proof | PM / Art Direction 判定 `approved` 或保留 `needs_fix`，不把 `production` 自动当 approved |

V02.22 的实施原则是先定边界、再拆场景、再接生活系统、最后用旧路径和 1280 proof 收口。每个拆分任务都必须保持当前 `world_map` cell 坐标、save key、logical asset ID、A-Z anchor ID / route_order 和孩子端真实入口不变；拆分后的 scene 只改变工程边界，不改变已通过的生活路径。

| 阶段 | 目标 | 完成门槛 |
|---|---|---|
| V02-HIDDENGRID-001 V02.22 路线、架构边界和任务包 | 固定隐藏网格生活小镇与 scenes 组件化的统一计划、禁改范围、scene 拆分顺序和验收矩阵 | `docs/12`、`docs/13`、`docs/14`、`docs/15`、`todo.md` 和 Round107 任务包同步；下一轮 Ready 明确；不改 runtime |
| V02-HIDDENGRID-002 TownStage scene 抽离 | 将运行时地图 canvas、底图、scenery、road hint、place marker、anchor marker、resource marker、NPC marker 和 player marker 从 `main.gd` 抽为 `scenes/world/town_stage.tscn` 与专用脚本 | 主场景仍从真实入口启动；地图缩放、点击到 cell、玩家连续行走、热点提示、26 anchor 和旧 smoke 通过 |
| V02-HIDDENGRID-003 Actor / Interactable scene 抽离 | 建立 `PlayerActor`、`NPCActor`、`InteractableObject` / `AnchorObject` / `ResourceObject` 等 prefab，统一占格、显示、交互优先级和 logical asset ID 接入 | NPC、资源、anchor、place 不互吞；A-Z 相册落账、资源同日去重、NPC 问候和旧路径回归通过 |
| V02-HIDDENGRID-004 UI scenes 抽离 | 将 TownHUD、TownFooter、Backpack / Shop / Home / Album / Settings 等高频 UI 从 `main.gd` 拆为可复用 scenes 或 panel scripts | 孩子端底栏、背包、商店、小屋、相册、设置关闭路径不退化；无课程 / 工程文案；focused UI tests 通过 |
| V02-HIDDENGRID-005 户外装饰、资源 2.0 与 NPC routine 隐藏网格纵切 | 在隐藏 cell 逻辑上扩展户外可摆物件、自然资源刷新、NPC 轻微 routine 和地点停留状态；孩子端不显示格子 | 至少一个户外装饰可摆 / 挪 / 收并保存；资源刷新仍温和；NPC routine 不阻断交互；A-Z anchor 稳定；不出现可见瓦块或坐标 |
| V02-HIDDENGRID-006 3-5 分钟回归、1280 proof 和阶段收口 | 用真实入口验证 scene 拆分和隐藏网格生活系统，导出 Town / Home / Shop / School line / Album / Settings 1280 proof | Headless runner、focused smoke、Godot 启动和 1280 proof 通过；PM / Art Direction 判定 approved / needs_fix；不把重构完成误标 product complete |
| V02-HIDDENGRID-007 Scene-native map + UI panel refactor | 将地图 authoring、TownStage runtime layers 和 Home / Shop / Backpack / Album / Settings panels 推进为可在 Godot 编辑器微调的 scene-native 工作流 | Authoring scene 导出 dictionary 并通过 `WorldMapContract`；runtime layer 分发和旧 facade 通过；panel scene script、旧节点名、入口和关闭路径通过；runtime JSON、save key、A-Z 和 logical asset ID 不变 |

| 阶段 | 目标 | 完成门槛 |
|---|---|---|
| V02-TEXTBOOK-001 小学全册教材源补齐 | 补齐三上，核对三下至六下现有 7 册 | 三上至六下 8 册均有本地摘要或来源记录 |
| V02-TEXTBOOK-002 全量课本内容结构化清单 | 将 8 册按单元整理为可加载 JSON | 85 个单元摘要有主题、关键词、短句和生活场景候选 |
| V02-TEXTBOOK-003 世界化主线映射表 | 将内容点映射到地点、NPC、anchor、故事记忆和回访路径 | 每条映射都有 `world_place_id`、`npc_id`、`anchor_id` 和孩子端温和表达 |
| V02-TEXTBOOK-004 Home / School 第一主线与 P0/P1/P2 分层 | P0 聚焦 0 基础与低年级高频，P1/P2 预留扩展 | P0 不用远郊 X/Z，不阻断当前每日小镇主路径 |
| V02-TEXTBOOK-005 数据合同与回归验证 | 新增 textbook world 合同、focused test 和 headless runner 注册 | JSON、合同、focused、headless runner 和 Godot 启动通过 |

V02.13 的收口证据包括 `curriculum/小学英语重点分析/三年级上册.txt`、`data/curriculum/textbook_world_plan.json`、`scripts/data/textbook_world_contract.gd`、`tests/test_v0213_textbook_world_plan.gd`、`tests/headless_runner.gd` 和 Round77 协作文档。V02.14 的执行入口是 `docs/collaboration/Round78_V02.14_HOMESCHOOL-001_PM执行任务包.md`；仍不得把课本主线写成课程地图或答题路线。

## 归档说明

以下原计划保留用于追溯早期技术架构、数据结构和已实现 stub。凡与当前路线冲突时，以 `todo.md` 和 `docs/12_V02开发路线.md` 为准。

> 项目：StudyGame V02
> 目标：把 V02 从产品框架推进到可执行开发计划，定义数据结构、系统接口、任务清单、文档要求、测试标准和远景开发方向。
> 范围：计划覆盖到第一阶段主循环可跑通，不在本阶段要求真实联网、真实 AI、商业化或完整内容库。

## 1. 阶段边界

V02 第一阶段的结束状态：

- 可以在 Godot 编辑器中编辑世界地图，并导出运行时 JSON。
- 运行时能从 JSON 生成世界地图、地点、道路、碰撞、交互点和 A-Z anchor。
- Home / Pet / Shop / Coins / Memory Card / Letter Snake 的最小主循环可跑通。
- Voice 和 AI 只使用本地 stub 跑通接口，不接真实 TTS、录音权限、跟读或大模型。
- 家长后台能读取本地学习摘要 stub，但不进入孩子主流程。
- 远景能力已有接口或数据预留，但不扩大第一阶段实现边界。

第一阶段不做：

- 真实账号。
- 云存档。
- 联网互访。
- 真实 AI NPC。
- 真实语音跟读评分。
- 商业化、内购、订阅。
- 多套美术主题。
- 完整教材内容库。

## 2. 数据架构总则

V02 采用混合数据模式：

- Godot `Resource` 负责编辑体验、Inspector 友好配置和 editor tool 操作。
- JSON 负责运行时读取、自动测试、内容包、AI 管线和未来后台同步。
- Editor tool 负责 Resource / EditorOnly 节点 / JSON 的同步。

数据原则：

- 所有核心对象必须有稳定 `id`。
- 运行时不依赖 EditorOnly 节点。
- JSON 是运行时事实来源。
- Resource 是编辑体验来源。
- 每个 schema 都要声明第一阶段是否实现，以及未来扩展方向。

## 3. 核心数据结构

### 3.1 `WorldMapResource` / `world_map.json`

用途：定义世界地图整体配置。

关键字段：

- `map_id`
- `version`
- `canvas_size`
- `cell_size`
- `theme_id`
- `districts`
- `roads`
- `places`
- `memory_anchors`
- `collision_cells`
- `interaction_cells`

第一阶段：实现。

验收：运行时可从 JSON 构建地图；editor tool 可从 Resource/节点导出 JSON。

### 3.2 `DistrictResource`

用途：定义 Home、Town、School、Transport 等区域。

关键字段：

- `district_id`
- `label`
- `rect_cells`
- `tile_id`
- `allowed_place_types`

第一阶段：实现 Home、Town、School、Transport。

验收：place 必须落在合法 district 内。

### 3.3 `RoadLayerResource`

用途：定义道路格和路线。

关键字段：

- `road_id`
- `road_type`
- `cells`
- `connects_to`

第一阶段：实现主路、商店街、交通路。

验收：shop 和 transport interaction cell 必须连接道路。

### 3.4 `PlaceResource`

用途：定义地点、地标、点击范围和玩法入口。

关键字段：

- `place_id`
- `label`
- `district_id`
- `position`
- `size`
- `landmark_asset_id`
- `occupied_cells`
- `interaction_cell`
- `place_action`
- `card_actions`
- `unlock_rule`

第一阶段：实现 Home、School、Supermarket、Pet Shop、Bookshop、Bus Station。

验收：视觉地标、点击区域、交互点和碰撞数据一致。

### 3.5 `MemoryAnchorResource`

用途：定义 A-Z 固定记忆宫殿锚点。

关键字段：

- `anchor_id`
- `letter`
- `core_word`
- `route_order`
- `place_id`
- `position`
- `card_id`
- `audio_id`
- `unlock_rule`

第一阶段：实现完整 A-Z 数据，运行时可先只启用部分互动。

验收：26 个 route_order 唯一，A = Apple 不可被覆盖。

### 3.6 `MemoryCardResource`

用途：定义单词记忆卡。

关键字段：

- `card_id`
- `letter`
- `word`
- `anchor_id`
- `image_asset_id`
- `audio_id`
- `story_memory`
- `collection_state`
- `source_curriculum_refs`
- `minigame_refs`

第一阶段：实现 A-Z core cards 和最小 Memory Album 数据。

验收：Letter Snake 结果能写入卡片状态。

### 3.7 `QuestEventResource`

用途：定义生活事件、地点委托、宠物需求、小游戏入口。

关键字段：

- `event_id`
- `title`
- `event_type`
- `start_place_id`
- `required_cards`
- `dialogue_id`
- `minigame_id`
- `rewards`
- `learning_tags`

第一阶段：实现宠物饥饿、买食物、Letter Snake 奖励链。

验收：孩子端不出现 lesson / test / review 等强学习表达。

### 3.8 `NPCProfileResource` / `NPCMemoryResource`

用途：定义 NPC 人设、固定对白、后期 AI 记忆。

关键字段：

- `npc_id`
- `display_name`
- `role`
- `personality`
- `voice_profile_id`
- `allowed_topics`
- `memory_file`
- `summary_enabled`

第一阶段：实现本地 profile 和 memory stub。

验收：NPC 可读取 profile；AI 接口未接入时使用固定对白。

### 3.9 `VoiceLineResource`

用途：定义 TTS、录音、后期跟读所需数据。

关键字段：

- `audio_id`
- `text`
- `tts_voice_id`
- `speaker_id`
- `recording_enabled`
- `repeat_after_me_enabled`
- `speech_eval_enabled`

第一阶段：只定义数据和 stub。

验收：VoiceService mock 可返回可播放占位结果。

### 3.10 `MinigameConfigResource`

用途：定义小游戏配置。

关键字段：

- `minigame_id`
- `type`
- `target_letters`
- `target_words`
- `card_rewards`
- `coin_rewards`
- `return_scene`

第一阶段：实现 `letter_snake` 配置。

验收：小游戏结果能返回 coins 和 card progress。

### 3.11 `LearningRecordResource`

用途：记录隐藏学习进展。

关键字段：

- `profile_id`
- `card_states`
- `word_exposures`
- `voice_attempts`
- `minigame_results`
- `npc_summary_refs`

第一阶段：本地保存，供家长后台摘要读取。

验收：孩子端不显示考核状态。

### 3.12 `ThemeProfileResource`

用途：支持统一美术风格和未来换肤。

关键字段：

- `theme_id`
- `tile_atlas`
- `npc_assets`
- `pet_assets`
- `ui_skin`
- `card_frame_assets`

第一阶段：只实现一个主题。

验收：运行时资源通过 AssetResolver 获取，不在玩法脚本里写死路径。

### 3.13 `FutureCapabilityFlags`

用途：记录远期预留能力。

关键字段：

- `account_ready`
- `cloud_save_ready`
- `ai_ready`
- `voice_eval_ready`
- `online_visit_ready`
- `multiplayer_ready`

第一阶段：只定义，不驱动真实功能。

## 4. 核心服务接口

### 4.1 `AssetResolver`

职责：根据 `theme_id` 和逻辑 id 返回资源路径。

第一阶段要求：

- 返回 NPC、pet、tile、card、UI skin 占位资源。
- 禁止 gameplay 脚本直接硬编码具体资产路径。

### 4.2 `SaveService`

职责：本地保存 profile、GameState、LearningRecord。

第一阶段要求：

- Local 实现。
- 预留未来 Cloud 实现。

### 4.3 `MapEditorSyncService`

职责：同步 EditorOnly 节点、Resource 和 JSON。

第一阶段要求：

- 从 JSON 导入编辑节点。
- 从编辑节点导出 JSON。
- 支持保存前校验。

### 4.4 `MapValidationService`

职责：校验地图数据。

第一阶段要求：

- place 在合法 district。
- interaction cell 不在 occupied cell。
- shop / transport 连接道路。
- A-Z 完整且 route_order 唯一。
- quest focus place 存在。

### 4.5 `RuntimeMapBuilder`

职责：从 JSON 生成运行时地图。

第一阶段要求：

- 生成 TileMapLayer。
- 生成 landmark sprites。
- 生成 collision。
- 生成 hotspot。
- 生成 A-Z anchor markers。

### 4.6 `QuestEventService`

职责：启动、完成和记录事件。

第一阶段要求：

- 支持宠物需求事件。
- 支持商店购买事件。
- 支持小游戏入口事件。

### 4.7 `MemoryCardService`

职责：管理卡片状态。

第一阶段要求：

- Seen / Heard / Played / Collected / Shiny 状态可写入。
- 小游戏结果能推进卡片状态。

### 4.8 `PetService`

职责：管理宠物状态和需求。

第一阶段要求：

- 饥饿状态。
- 食物消耗。
- 开心反馈。
- 与 Shop/Quest/MemoryCard 奖励链连接。

### 4.9 `ShopService`

职责：购买食物、玩具、装饰。

第一阶段要求：

- coins 支付。
- 食物购买。
- 购买结果写入本地状态。

### 4.10 `MinigameService`

职责：启动小游戏、接收结果、发放奖励。

第一阶段要求：

- 启动 Letter Snake。
- 接收 score 和 target word 结果。
- 转换成 coins 和 card progress。

### 4.11 `VoiceService`

职责：TTS、录音、跟读预留。

第一阶段要求：

- mock TTS。
- mock recording。
- 不申请真实移动端权限。

### 4.12 `LLMClient`

职责：通用大模型接口。

第一阶段要求：

- stub 回复。
- 不接真实模型。
- 保留 prompt input/output 类型。

### 4.13 `NPCMemoryStore`

职责：读写 NPC 记忆文件。

第一阶段要求：

- 本地 stub。
- 可记录最近事件摘要。

### 4.14 `ParentDashboardStore`

职责：提供家长后台摘要数据。

第一阶段要求：

- 从 LearningRecord 和 NPC summary stub 读取本地摘要。

## 5. 任务级开发清单

### 5.1 项目骨架

| Task ID | Owner | Goal | Outputs | Acceptance Criteria |
|---|---|---|---|---|
| V02-CORE-001 | Godot Dev Agent | 创建 Godot 4.x 移动端项目骨架 | `project.godot`、基础目录 | 项目可在 Godot 打开 |
| V02-CORE-002 | Godot Dev Agent | 建立 scenes/scripts/data/assets/tests 目录 | 目录结构 | 路径符合技术架构 |
| V02-CORE-003 | Godot Dev Agent | 配置共享工具路径和 Git 忽略规则 | `.gitignore`、共享工具说明、路径模板 | 生图脚本和 MCP 服务不进入 GitHub |
| V02-CORE-004 | Godot Dev Agent | 建立 Local SaveService stub | SaveService 脚本 | 可读写本地 profile stub |
| V02-CORE-005 | Asset Agent | 建立 ThemeProfile 和 AssetResolver 占位 | theme 数据和 resolver | 运行时不硬编码资产路径 |

### 5.2 地图编辑工具

| Task ID | Owner | Goal | Outputs | Acceptance Criteria |
|---|---|---|---|---|
| V02-MAP-001 | Map Tool Agent | 定义 WorldMap/Place/Road/District Resource | Resource 脚本和 JSON 示例 | Resource 可序列化为 JSON |
| V02-MAP-002 | Map Tool Agent | 建立 EditorOnly proxy 节点结构 | WorldOverviewScene 编辑层 | 可拖 place marker |
| V02-MAP-003 | Map Tool Agent | 实现 grid overlay | 编辑器网格显示 | 触屏逻辑 cell 与编辑 cell 一致 |
| V02-MAP-004 | Map Tool Agent | 实现 road cell 编辑 | road 编辑工具 | 可添加/删除 road cell |
| V02-MAP-005 | Map Tool Agent | 实现 occupied / interaction cell 编辑 | 碰撞与交互编辑工具 | interaction 不允许落在 occupied |
| V02-MAP-006 | Map Tool Agent | 实现撤销/重做 | editor command stack | 地图编辑操作可撤销 |
| V02-MAP-007 | Map Tool Agent | 实现导入/导出 JSON | sync service | JSON 往返后数据一致 |
| V02-MAP-008 | QA Agent | 实现地图校验脚本 | map validation test | 错误数据可被拦截 |

### 5.3 运行时地图

| Task ID | Owner | Goal | Outputs | Acceptance Criteria |
|---|---|---|---|---|
| V02-RUNTIME-001 | Godot Dev Agent | 实现 RuntimeMapBuilder | 地图生成器 | 可从 JSON 生成地图 |
| V02-RUNTIME-002 | Godot Dev Agent | 生成 TileMapLayer 和 roads | runtime tiles | 地面和道路可见 |
| V02-RUNTIME-003 | Godot Dev Agent | 生成 landmark、hotspot、collision | runtime objects | 点击和碰撞与数据一致 |
| V02-RUNTIME-004 | Godot Dev Agent | 生成 A-Z anchor markers | anchor layer | 26 个 anchor 数据完整 |

### 5.4 A-Z 与记忆卡

| Task ID | Owner | Goal | Outputs | Acceptance Criteria |
|---|---|---|---|---|
| V02-AZ-001 | Memory Palace Agent | 从 A-Z 策划提取 core anchor 表 | A-Z 数据表 | A = Apple，route_order 1-26 唯一 |
| V02-AZ-002 | Curriculum Agent | 绑定 0 基础词和教材来源 | curriculum refs | 每张 core card 有来源或补齐标记 |
| V02-CARD-001 | Godot Dev Agent | 实现 MemoryCardService | card 状态服务 | 可写入 Seen/Played/Collected |
| V02-CARD-002 | UI/UX Agent | 设计 Memory Album 第一版 | UI 规格 | 孩子端像相册，不像单词表 |

### 5.5 Home / Pet / Shop / Coins 主循环

| Task ID | Owner | Goal | Outputs | Acceptance Criteria |
|---|---|---|---|---|
| V02-LOOP-001 | Game Design Agent | 定义宠物饥饿到买食物循环 | 事件设计 | 不出现强考核表达 |
| V02-PET-001 | Godot Dev Agent | 实现 PetService | pet 状态服务 | 宠物可饿、可喂、可反馈 |
| V02-SHOP-001 | Godot Dev Agent | 实现 ShopService | shop 购买服务 | coins 可购买食物 |
| V02-QUEST-001 | Godot Dev Agent | 实现 QuestEventService 最小版 | 事件服务 | 宠物需求可触发小游戏入口 |
| V02-UI-001 | UI/UX Agent | 设计触屏 Home/Pet UI | UI 规格 | 手机和平板触控区域足够 |

### 5.6 Letter Snake 小游戏

| Task ID | Owner | Goal | Outputs | Acceptance Criteria |
|---|---|---|---|---|
| V02-MINI-001 | Minigame Agent | 定义 Letter Snake 规则 | minigame config | 支持 target letters/words |
| V02-MINI-002 | Godot Dev Agent | 实现 MinigameService | minigame service | 可启动并返回结果 |
| V02-MINI-003 | Godot Dev Agent | 实现 Letter Snake 原型 | minigame scene | 分数转换 coins/card progress |
| V02-MINI-004 | QA Agent | 测试奖励差异 | tests | 低分有奖励，高分奖励更多 |

### 5.7 语音与 AI 本地桩

| Task ID | Owner | Goal | Outputs | Acceptance Criteria |
|---|---|---|---|---|
| V02-VOICE-001 | Voice/AI Agent | 定义 VoiceLine schema | voice 数据 | 支持 TTS/录音/跟读字段 |
| V02-VOICE-002 | Godot Dev Agent | 实现 VoiceService stub | voice service | mock 播放不报错 |
| V02-AI-001 | Voice/AI Agent | 定义 NPC profile/memory schema | NPC 数据 | NPC 有 profile 和 memory 文件 |
| V02-AI-002 | Godot Dev Agent | 实现 LLMClient stub | AI client | 返回固定安全占位回复 |
| V02-AI-003 | Godot Dev Agent | 实现 ConversationSummary stub | summary service | 家长后台可读取摘要 stub |

### 5.8 家长后台本地摘要

| Task ID | Owner | Goal | Outputs | Acceptance Criteria |
|---|---|---|---|---|
| V02-PARENT-001 | UI/UX Agent | 定义家长入口 | UI 规格 | 不进入孩子主流程 |
| V02-PARENT-002 | Godot Dev Agent | 实现 ParentDashboardStore stub | dashboard 数据服务 | 可读取 card/minigame/NPC 摘要 |
| V02-PARENT-003 | QA Agent | 校验孩子端不显示后台内容 | test | 主流程无家长报告 UI |

## 5.9 当前阶段增补：V02.27 Map Editor 完整化

V02.27 已把早期“编辑器工具能写回 JSON”的粗粒度目标细化为场景内 Map Editor 完整化。`TownMapAuthoring` 继续作为 Godot 场景内工具，不做 EditorPlugin，不进入孩子端 runtime；运行时仍只读取正式 JSON。

| Task ID | Owner | Goal | Outputs | Acceptance Criteria |
|---|---|---|---|---|
| V02-MAPEDITOR-001 | PM / Tooling / QA | 固定路线、禁改边界和任务包 | docs 12-15、协作记录、`todo.md` | 任务队列和分文件保存策略同步 |
| V02-MAPEDITOR-002 | Tooling / UI / QA | 图层开关、选择态和 Inspector | toolbar、layer toggles、tool mode、Inspector、status area | 七类图层可独立显示隐藏，marker 可点选查看 |
| V02-MAPEDITOR-003 | Tooling / Map / QA | Place 拖动、属性编辑和 Save Map | place field editing、linked move、map save | 保存前强制 Validate，合法才写 map 目标 |
| V02-MAPEDITOR-004 | Tooling / Map / QA | Road / collision / interaction cell painting | 单 cell 画笔和橡皮 | 只改候选 state，不覆盖 protected cells |
| V02-MAPEDITOR-005 | Tooling / Economy / QA | Resource marker 编辑和保存 | resource marker move / fields / validate / save | `item_id` 来自 catalog，合法才写 resource target |
| V02-MAPEDITOR-006 | Tooling / NPC / QA | NPC routine day selector、marker 编辑和保存 | day selector、routine move / label / validate / save | `npc_id` 已知，arrival safe，合法才写 routine target |
| V02-MAPEDITOR-007 | Tooling / Memory Palace / QA | A-Z anchor 迁移保护和回归 | move-anchor mode、locked fields、full regression | 26 anchors 完整，核心字段不可编辑，V02.17 / V02.18 / V02.25 / V02.26 回归通过 |
| V02-MAPEDITOR-008 | Tooling / UX / QA | Map Editor 可用性降噪 | stable map canvas、top toolbar band、right inspector/status rail、compact marker badges | 打开 `town_map_authoring.tscn` 时面板不再分散压住地图；marker 标签短且可读；保存合同不变 |
| V02-MAPEDITOR-009 | Tooling / UX / QA | Map Editor 视觉布局二次整理 | left control rail、fixed-height panels、right map canvas、sidebar text clipping | 1280x720 下工具不横向溢出，侧栏不扩成超高黑块，完整地图 canvas 不裁切；保存合同不变 |
| V02-MAPEDITOR-010 | Tooling / UX / QA | Map Editor 直接拖拽编辑与字母可见性返修 | global drag tracking、Inspector input fields + Apply、readable A-Z markers | marker 拖拽通过候选验证提交，Inspector 可直接编辑字段，A-Z 字母清晰可见且 anchor 迁移仍需 Move A-Z 模式；保存合同不变 |

验收测试：

- `tests/test_v027_mapeditor_layers_inspector.gd`
- `tests/test_v027_mapeditor_place_save.gd`
- `tests/test_v027_mapeditor_cell_painting.gd`
- `tests/test_v027_mapeditor_resource_save.gd`
- `tests/test_v027_mapeditor_npc_routine_save.gd`
- `tests/test_v027_mapeditor_anchor_migration_guard.gd`
- `tests/test_v027_mapeditor_full_regression.gd`
- `tests/test_v027_mapeditor_usability_declutter.gd`
- `tests/test_v027_mapeditor_direct_edit_drag.gd`
- `tests/headless_runner.gd`

## 5.10 当前阶段增补：V02.28 Map Editor 实战生产验证

V02.28 不新增孩子端系统，不重构 Map Editor，不扩远郊 P0。目标是用 V02.27 已完成的场景内 Map Editor 做一次小批量真实生产，证明编辑器、分文件保存、运行时加载、孩子端真实入口和 1280 proof 能组成稳定内容生产链。

| Task ID | Owner | Goal | Outputs | Acceptance Criteria |
|---|---|---|---|---|
| V02-MAPDOGFOOD-001 | PM / Tooling / QA | 固定路线、禁改边界和任务包 | docs 12-15、协作记录、`todo.md` | 下一项 Ready 明确；本轮不改 runtime/data/tests |
| V02-MAPDOGFOOD-002 | Tooling / Map / QA | Map Editor 小批量生产实战 | 少量 Place / resource / NPC routine 正式数据改动 | 改动由 Map Editor 候选 state 经 Validate 和安全写回产生；JSON 可加载；A-Z 不退化 |
| V02-MAPDOGFOOD-003 | QA / UX / Child Safety | 孩子端真实入口复核 | focused smoke、儿童文本检查、相册 / 互动路径复核 | dogfood 内容可见可达，不遮挡核心入口，不显示编辑器 / 坐标 / 格子术语 |
| V02-MAPDOGFOOD-004 | Tooling / PM / Content | 内容生产手册与操作边界 | Map Editor 操作手册、保存流程、禁改字段清单 | 后续内容生产可按手册执行；A-Z 稳定 ID / letter / core_word / route_order 保护规则清楚 |
| V02-MAPDOGFOOD-005 | QA / PM | 回归、1280 proof 与阶段收口 | focused tests、full runner、Godot 启动、1280 proof、收口记录 | 证明 Map Editor 可用于生产；不标 product complete；960x540 留到版本适配专项 |

V02.28 验收必须保留 V02.27 的分文件保存边界：`world_map.json`、`resource_points.json` 和 `npc_routines.json` 只能由各自验证路径写入。任何绕过 `MapEditorSyncService` 的手工 JSON 修改，只能作为调试，不能算 dogfood 交付。

## 5.11 当前阶段增补：V02.29-V02.36 故事 / 动作 / 操控 / 美术长远路线

V02.29-V02.36 是一组多轮长线阶段，不是单轮内容扩展。目标是先让故事线、当前地图、动作规格、操控规格、美术生产和运行时纵切形成顺序清楚的生产线，再进入批量资产和故事实现。

| 版本 | 主题 | 主要交付 | 阶段门槛 |
|---|---|---|---|
| V02.29 | 长远路线与故事总纲 | V02.29-V02.36 路线、故事总纲、章节分层、A-Z 故事索引 | Round141 任务包完成；Round142 故事总纲完成；后续 Round143 / Round144 已完成地图适配与动作规格 |
| V02.30 | 当前地图适配与内容生产蓝图 | story-place-anchor-NPC 绑定表、Map Editor 候选清单、P0/P1/P2 缺口 | Round143 已完成；不重排 26 A-Z，不扩远郊 P0 |
| V02.31 | 行走动画与角色动作规格 | player / NPC / Sunny sprite sheet、状态机、4 向基础动作、8 向预留 | Round144 已完成；帧数、尺寸、pivot、命名、loop、logical animation ID 和 proof 口径清楚 |
| V02.32 | Animal Crossing-like 操控规格 | 连续移动、隐藏网格、镜头跟随、靠近 prompt、输入策略 | Round145 已完成；可直接拆给 Godot；孩子端不显示格子 / 坐标 / 编辑器术语 |
| V02.33 | Tile map / 场景 / 故事资产生产规格 | tile、edge、prop、story object、character motion、UI feedback backlog | Round146 已完成；每项有 logical asset ID、用途、尺寸、依赖故事线和截图点 |
| V02.34 | 首批美术生产与编辑器接入 | tile / prop / story object / animation 资产、ThemeProfile / AssetResolver 映射 | Round148 已完成 production 接入；approved 仍需 1280 proof 和 PM / Art Direction 判定 |
| V02.35 | 动作操控运行时纵切 | 连续移动、4 向动画、镜头、prompt、真实入口 smoke | 1280 proof 和移动 / prompt 稳定性采样通过 |
| V02.36 | 故事线 + 美术 + 动作整合纵切 | P0 故事章节、A-Z 回访、相册落账、NPC / resource / story object 联动 | Round150 阶段 RC proof 通过；可进入后续内容批量生产 |

本长线阶段的禁改范围：不重排 A-Z anchor，不把 School 做成课程 / 考试空间，不把 Bus Stop / Bookshop / far edge 扩成 P0 必经路线，不硬编码素材路径，不把 Map Editor 或网格术语暴露到孩子端。

## 5.12 当前阶段增补：V02.37+ 故事内容批量生产与批准线

V02.37+ 的目标是把 Round150 的 story slice 变成可重复生产的内容队列。第二批内容优先补 Round150 未覆盖的 P0 / first-ring anchor，继续保持 English 为环境层和相册层，不成为课程或主循环门槛。

| Task ID | Owner | Goal | Outputs | Acceptance Criteria |
|---|---|---|---|---|
| V02-STORYBATCH-001 | PM / Narrative / Art Direction / QA | 固定 V02.37+ 路线、禁改边界和批准线 | docs 12-15、Round151 任务包、`todo.md` | 下一项 Ready 明确；本轮不改 runtime/data/tests/assets |
| V02-STORYBATCH-002 | Narrative / Memory Palace / Art Direction / QA | 第二批 story prop / A-Z 回访内容包 | C/D/G/K/O/S 优先的内容候选、story memory、visual hook、review path、asset needs | 不复用覆盖同一 `core_anchor_id`；不课程化；不扩远郊 P0 |
| V02-STORYBATCH-003 | Asset / Tech Art / Godot / QA | 第二批 production 资产与接入 | story prop / anchor refresh assets、logical asset ID、ThemeProfile / AssetResolver 映射 | `production` 不自动 approved；不硬编码路径 |
| V02-STORYBATCH-004 | Godot / Narrative / QA | 第二批 runtime 接入与真实入口 smoke | runtime story props、story slice save record、A-Z card state、focused smoke | 阻塞；等待 V02.39 达到 `runtime_visual_match` 并由 PM / Art Direction 明确解锁 |
| V02-STORYBATCH-005 | QA / PM / Art Direction | 1280 proof、approved 判定与阶段收口 | focused/full runner、JSON、diff check、Godot 启动、1280 proof、判定记录 | 阻塞；不得在 `visual_scaffold` 上做 approved 判定，需等待 V02.39 final gate |

V02.37+ 禁止把已完成的 production 资产、headless 通过或旧 proof 自动升级为 approved。V02.39 起 `visual_candidate` 也不得仅由工程 gate 授予；恢复 story batch 后必须按 `functional_pass`、`visual_scaffold`、`art_target_locked`、`runtime_visual_match`、`final_approved` 新语义重新判定。

## 6. 远景开发方向规划

### 6.1 AI NPC 与记忆系统

路线：

1. 本地 fixed dialogue。
2. LLMClient stub。
3. 通用大模型接入。
4. NPC 独立记忆文件。
5. 家长摘要。
6. 多 NPC 长期关系。
7. AI 辅助生成生活事件和复现任务。

第一阶段只做 1-2。

### 6.2 英语语音环境与跟读

路线：

1. VoiceLine 数据。
2. TTS stub。
3. 录音 stub。
4. 真实 TTS。
5. 真实录音权限。
6. 跟读 AI。
7. 发音反馈。
8. NPC 语音互动。

第一阶段只做 1-3。

### 6.3 内容生产与 AI 辅助管线

路线：

1. 使用现有 curriculum。
2. 补齐 0 基础内容。
3. 建立 curriculum -> scene -> card -> minigame 映射。
4. AI 生成候选内容。
5. 规则校验。
6. 人工确认。
7. 内容包发布。

第一阶段做到数据结构和少量手工内容。

### 6.4 账号、云存档与家长后台

路线：

1. 本地 profile。
2. AccountAdapter 预留。
3. Cloud Save。
4. Web/移动家长后台。
5. 多设备同步。

第一阶段只做本地 profile 和接口预留。

### 6.5 小朋友互访与受控多人互动

路线：

1. 本地单机。
2. 异步访问朋友房间或小镇角落。
3. 家长批准好友。
4. 预设短语和表情。
5. 受控实时合作小游戏。
6. AI NPC 组织多人任务。

第一阶段只保留数据和服务接口，不实现联网。

### 6.6 美术主题换肤与平台扩展

路线：

1. 单一 Animal Crossing-like cozy town / Apple-like translucent glass UI 风格。
2. ThemeProfile。
3. AssetResolver。
4. 第二套主题。
5. 主题包下载。
6. Android / iOS / 平板适配深化。

第一阶段只做一个主题和换肤接口。

## 7. 子 Agent 分工

所有任务必须使用 `docs/collaboration/任务交接模板.md` 分配。

必读文档：

- `docs/01_产品总纲.md`
- `docs/03_英语学习与0基础进阶体系.md`
- `docs/04_A-Z记忆宫殿与记忆卡系统.md`
- `docs/05_世界结构与地图编辑架构.md`
- `docs/11_Godot技术架构.md`
- `docs/13_V02详细开发计划.md`

每个子 agent 必须提交：

- 修改文件。
- 新增内容。
- 验证方式。
- 风险。
- 待确认问题。

## 8. 测试与验收标准

第一阶段必须建立以下测试：

- schema load tests。
- JSON round-trip tests。
- map validation tests。
- runtime map smoke tests。
- A-Z uniqueness tests。
- Memory Card state tests。
- Pet/Shop/Coins loop tests。
- Minigame reward tests。
- Voice/AI stub tests。
- Parent dashboard visibility tests。

验收总标准：

- 孩子端没有强考核、课程表、家长报告。
- A-Z 核心编码不被推翻。
- 运行时数据来自 JSON。
- 编辑器工具能写回 JSON。
- 主循环可跑通。
- 远景能力已预留但不扩大首期实现。
